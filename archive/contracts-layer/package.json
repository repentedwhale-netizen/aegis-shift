{
  "name": "@aegis-shift/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "test:gas": "forge test -vvv --gas-report",
    "lint": "solhint 'src/**/*.sol'",
    "coverage": "forge coverage",
    "deploy": "forge script script/Deploy.s.sol --broadcast",
    "anvil": "anvil --block-time 2"
  },
  "devDependencies": {
    "solhint": "^4.5.2"
  }
}
